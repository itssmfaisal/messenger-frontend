# Set these to the address your BROWSER can reach the backend at.
# Use your server's public IP or domain name — NOT localhost (unless running locally).
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_WS_URL=http://localhost:8080/ws

# NEXT_PUBLIC_API_URL=https://messenger.otaworkstation.shop:8080
# NEXT_PUBLIC_WS_URL=https://messenger.otaworkstation.shop:8080/ws

